# Local one-click stack for AIBDSC.
# Client only needs Docker Desktop; everything else runs in containers.
# All values have sane defaults, so a missing .env still works.
name: aibdsc
services:
  db:
    image: postgres:16-alpine
    container_name: aibdsc-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aibdsc}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aibdsc_local_password}
      POSTGRES_DB: ${POSTGRES_DB:-aibdsc}
    volumes:
      - aibdsc-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aibdsc} -d ${POSTGRES_DB:-aibdsc}"]
      interval: 5s
      timeout: 5s
      retries: 15

  backend:
    build: ./backend
    container_name: aibdsc-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Backend reaches the DB by the compose service name "db", never localhost.
      DATABASE_URL: postgresql://${POSTGRES_USER:-aibdsc}:${POSTGRES_PASSWORD:-aibdsc_local_password}@db:5432/${POSTGRES_DB:-aibdsc}
      JWT_SECRET: ${JWT_SECRET:-change-me-local-dev-secret}
      NODE_ENV: production
      FRONTEND_URL: http://localhost:${FRONTEND_PORT:-5173}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@aibdsc.local}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin12345}
      ADMIN_GYM_NAME: ${ADMIN_GYM_NAME:-AIBDSC}
    ports:
      - "${BACKEND_PORT:-3001}:3001"

  frontend:
    build: .
    container_name: aibdsc-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      # Browser (host) talks to the backend on localhost, not the "backend" service name.
      VITE_API_URL: http://localhost:${BACKEND_PORT:-3001}
    ports:
      - "${FRONTEND_PORT:-5173}:5173"

volumes:
  aibdsc-db-data:
